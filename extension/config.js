// Update these after you deploy the backend to Vercel.
// If you deploy to a custom domain instead of *.vercel.app, also add that
// domain to "host_permissions" in manifest.json or the extension won't be
// allowed to call it.
const SKVK_CONFIG = {
  API_BASE: 'https://system-al.vercel.app//api',
  DASHBOARD_URL: 'https://system-al.vercel.app'
};
